#include <iostream>

int main() {
	Cluster cluster;
	cluster.inicializa();
	//inicializa un area de procesos pendientes
	string comando;
	cin >> comando;
	while (comando != "fin") {
	  if (comando)
	}
}